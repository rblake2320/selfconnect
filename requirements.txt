# Core SDK dependencies
Pillow>=10.0.0
psutil>=5.9.0

# Optional: UIA text extraction (needed for get_text_uia, verify_delivery)
# pywinauto>=0.6.8
# comtypes>=1.4.0

# Optional: OCR-based delivery verification (needed for verify_delivery OCR path)
# pytesseract>=0.3.10
