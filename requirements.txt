# Core SDK dependencies
Pillow>=10.0.0
psutil>=5.9.0

# UIA text extraction (needed for get_text_uia, approval_partner, antigravity_controller)
pywinauto>=0.6.8
comtypes>=1.4.0

# Optional: OCR-based delivery verification (needed for verify_delivery OCR path)
# pytesseract>=0.3.10

# Optional: Telegram approval bridge (needed for approval_telegram.py)
# python-dotenv>=1.0.0
# python-telegram-bot>=22.0
